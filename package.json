{
  "name": "go3net-hrm-system",
  "version": "1.0.0",
  "description": "Go3net HR Management System",
  "private": true,
  "scripts": {
    "vercel-build": "cd frontend && npm install && npm run build"
  }
}
